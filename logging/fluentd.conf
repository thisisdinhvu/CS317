<source>
  @type tail
  path /fluentd/log/api.log       
  pos_file /fluentd/log/diabetes.pos
  tag diabetes-api
  format none
</source>

# <source>
#   @type forward
#   port 24224
#   bind 0.0.0.0
# </source>

<match **>
  @type stdout
</match>
